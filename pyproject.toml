[tool.poetry]
name = "miracle-box"
version = "0.1.0"
description = "Miracle Box Project"
authors = ["Ian Esteves <ianesteves@hotmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
